apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: demo-team-updater
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: "demo-app*"
      useAnnotations: true